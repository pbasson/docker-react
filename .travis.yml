language: generic
sudo: required
services:
    - docker
    
before_install: 
    - docker build -t pbasson/docker-react -f Dockerfile.dev .
    
script:
    -   docker run -e CI=true pbasson/docker-react npm run test -- --coverage
